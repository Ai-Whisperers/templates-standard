apiVersion: v1
kind: ConfigMap
metadata:
  name: ${APP_NAME}-config
  namespace: ${NAMESPACE}
data:
  # Application configuration
  # Add your environment variables here
  # Example:
  # APP_ENV: "production"
  # LOG_LEVEL: "info"
  # MAX_CONNECTIONS: "100"
